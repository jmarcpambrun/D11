langcode: en
status: true
dependencies:
  enforced:
    module:
      - ai_ckeditor
id: ai_ckeditor_modify__default
label: 'Default prompt for modifying CKEditor text'
type: ai_ckeditor_modify
prompt: |
  Use the following instructions to process this text. Return only the processed text without explanations.
  Instructions: {modifyPrompt}
  Text to process:
  {inputText}
