langcode: en
status: true
dependencies:
  enforced:
    module:
      - ai_ckeditor
id: ai_ckeditor_reformat__default
label: 'Default prompt for reformatting CKEditor text'
type: ai_ckeditor_reformat
prompt: |
  Please fix this text to be marked up with semantic HTML using only lists, headers, or paragraph tags:
  {inputText}
