{#
/**
 * @file
 * Full view mode template for Burndown task entities.
 *
 * This template intentionally renders the entity field content so site builders
 * can control visibility and ordering via Manage Display.
 */
#}
<div{{ attributes.addClass('burndown-task', 'burndown-task--full') }}>
  {{ content }}
</div>
