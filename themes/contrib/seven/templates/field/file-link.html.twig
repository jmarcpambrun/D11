{#
/**
 * @file
 * Theme override for a link to a file.
 *
 * Available variables:
 * - attributes: The HTML attributes for the containing element.
 * - link: A link to the file.
 * - file_size: The size of the file.
 *
 * @see \Drupal\image\Hook\ImageThemeHooks::preprocessImageWidget()
 * @see \Drupal\seven\Hook\SevenThemeHooks::preprocessFileLink()
 */
#}
{{ attach_library('seven/classy.file') }}
<span{{ attributes }}>{{ link }}{% if file_size %} <span class="file__size">({{ file_size }})</span>{% endif %}</span>
