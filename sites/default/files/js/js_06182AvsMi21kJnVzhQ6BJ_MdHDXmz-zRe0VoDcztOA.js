/* @license GPL-2.0-or-later https://www.drupal.org/licensing/faq */
Drupal.PrivateMessageNotificationBlock={};(($,Drupal,drupalSettings,window)=>{let initialized;let notificationWrapper;let refreshRate;let checkingCount;function triggerCommands(data){const ajaxObject=Drupal.ajax({url:'',base:false,element:false,progress:false});ajaxObject.success(data,'success');}function updateCount(unreadItemsCount){notificationWrapper=document.querySelector('.private-message-notification-wrapper');if(unreadItemsCount)notificationWrapper.classList.add('unread-threads');else notificationWrapper.classList.remove('unread-threads');notificationWrapper.querySelector('.private-message-page-link').textContent=unreadItemsCount;const pageTitle=document.querySelector('head title');if(unreadItemsCount)if(pageTitle.textContent.match(/^\(\d+\)\s/))pageTitle.textContent=pageTitle.textContent.replace(/^\(\d+\)\s/,`(${unreadItemsCount}) `);else pageTitle.textContent=`(${unreadItemsCount}) ${pageTitle.textContent}`;else{if(pageTitle.textContent.match(/^\(\d+\)\s/))pageTitle.textContent=pageTitle.textContent.replace(/^\(\d+\)\s/,'');}}function getUnreadItemsCount(){if(!checkingCount){checkingCount=true;$.ajax({url:drupalSettings.privateMessageNotificationBlock.newMessageCountCallback,success(data){triggerCommands(data);checkingCount=false;if(refreshRate)window.setTimeout(getUnreadItemsCount,refreshRate);}});}}Drupal.PrivateMessageNotificationBlock.getUnreadItemsCount=()=>{getUnreadItemsCount();};function init(){if(!initialized){initialized=true;if(drupalSettings.privateMessageNotificationBlock.ajaxRefreshRate){refreshRate=drupalSettings.privateMessageNotificationBlock.ajaxRefreshRate*1000;if(refreshRate)window.setTimeout(getUnreadItemsCount,refreshRate);}}}Drupal.behaviors.privateMessageNotificationBlock={attach(){init();Drupal.AjaxCommands.prototype.privateMessageUpdateUnreadItemsCount=(ajax,response)=>{updateCount(response.unreadItemsCount);};}};})(jQuery,Drupal,drupalSettings,window);;
